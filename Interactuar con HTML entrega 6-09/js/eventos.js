document.addEventListener('DOMContentLoaded', function() {
    M.AutoInit();
    });

    // const $linkform = document.getElementsByClassName("link-form");

    // /* $linkform.setAttribute("target", _blank); */
    // console.log($linkform);